An index trades slower writes for a search that halves.

It isn't a shortcut into your table. It's a second copy of one column, kept sorted —
and sorted is the whole trick, because sorted lets you throw away half the rows per step.

#database #sql #postgres #backend #systemdesign #programming #softwareengineering #devtips
