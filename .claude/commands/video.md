---
description: Make an Instagram explainer video end to end with the critic loop. Usage - /video <topic> [remotion|manim]
---

Topic and optional engine: $ARGUMENTS

Follow the Workflow in CLAUDE.md exactly, in order, without skipping the self-check in step 5.

Before writing the script, decide in two sentences what the one surprising idea is and what the single hero
diagram is. Put both in BRIEF.md. If the engine was not given, choose it using the Engine choice section and
say why.

Report back with: slug, engine, line count and total duration, the score for each critic round, and the path
to final.mp4.
